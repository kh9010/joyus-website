# macOS
.DS_Store

# Agemo rebuild scratchpad
.agemo-rebuild/

# Site rebuild scratchpad (plan doc, transcripts, agent notes)
.site-rebuild/

# Podcast auto-updater transient work files (the .sync-state.json IS committed)
scripts/.podcast-sync-pending.json
scripts/.podcast-sync-drafts.json
